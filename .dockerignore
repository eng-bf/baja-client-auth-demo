# Build context excludes — note .env is intentionally NOT ignored:
# Vite inlines VITE_* vars at build time, so .env must reach the build stage.
node_modules
dist
.git
.vscode
*.log
.DS_Store
Dockerfile
.dockerignore
deploy.sh
README.md
